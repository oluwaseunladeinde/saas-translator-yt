import React from 'react'

type Props = {}

const ShareLink = ({ isOpen, chatId }: {
    isOpen: boolean;
    chatId: string;
}) => {
    return (
        <div>ShareLink</div>
    )
}

export default ShareLink